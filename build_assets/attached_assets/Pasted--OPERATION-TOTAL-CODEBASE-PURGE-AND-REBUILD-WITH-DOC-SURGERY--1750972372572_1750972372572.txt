# OPERATION: TOTAL CODEBASE PURGE-AND-REBUILD — WITH DOC SURGERY

──────────────────────────────────────────
0. GROUND RULES
──────────────────────────────────────────
• Treat every *.md as UNTRUSTED, **except** we will mine
  `/mnt/data/executive_board_report3.md` for headings & prose
  that deserve to survive.  
• File `.replit` is holy. Touch it and you die.  
• Fail loud; print full stack traces — no silent flops.

──────────────────────────────────────────
1. GLOBAL CRAWL
──────────────────────────────────────────
1. Recursively scan repo for source files  
   (`*.py, *.js, *.ts, *.html, *.sql, *.yaml, *.mdx`).  
2. Build AST graph mapping:  
   ─ routes / blueprints / sockets / CLI cmds  
   ─ models / schemas / migrations  
   ─ scheduled / async jobs  
   ─ front-end components hitting those endpoints  
   ─ config flags & feature toggles  
3. Dump graph → `/tmp/codegraph.json`.

──────────────────────────────────────────
2. DUPLICATE & DEAD-WEIGHT PURGE
──────────────────────────────────────────
1. Hash-digest every file; flag >90 % twins.  
2. Keep newest, shortest-path copy; ref the rest via import
   or symlink.  
3. Delete modules never referenced anywhere in graph.

──────────────────────────────────────────
3. FUNCTIONAL REPAIR LOOP
──────────────────────────────────────────
Repeat ≤5 laps or until linters pass:  
1. Run `flake8`, `black --check`, `mypy`, `bandit`.  
2. Auto-patch obvious fails: dead imports, type flubs, security stink.  
3. Spin / create `pytest` suite; all green or keep looping.

──────────────────────────────────────────
4. CHAT-FIRST UNIFICATION
──────────────────────────────────────────
1. Forge **/core/chat** blueprint (SocketIO + REST fallback).  
2. Create **/api/chat** dispatcher that:  
   • Loads `codegraph.json`.  
   • Auto-registers ANY function/class tagged
     `@chat_handler`, `cmd_*`, or `handle_*`.  
   • Routes incoming messages by intent pattern — **no hard-coding.**  
3. Auto-generate stub chat-handlers for features lacking one; mark TODO.

──────────────────────────────────────────
5. DOCUMENTATION REBUILD & MERGE
──────────────────────────────────────────
1. Parse **/mnt/data/executive_board_report3.md**:  
   • Keep intro, mission statements, legal notes, etc.  
   • Yank its stale “feature index” & any dup sections.  
2. Overwrite **“🔎 Complete Feature Index (Auto-Generated)”** with
   fresh data from `codegraph.json` (alphabetical, ≤80 chars width).  
3. Ensure repo now holds exactly one copy each of:  
   • README.md            • CHANGELOG.md  
   • ARCHITECTURE.md      • API_REFERENCE.md (OpenAPI auto-export)  
   • CONTRIBUTING.md      • CODE_OF_CONDUCT.md  
   • LICENSE              • Any compliance docs (HIPAA, GDPR, etc.)  
4. Any headings or prose salvaged from the old MD go into the
   proper doc (e.g. “Vision” → README).  
5. Delete orphan *.md duplicates; list purged filenames in console.

──────────────────────────────────────────
6. CI GUARDRAILS
──────────────────────────────────────────
1. Drop `.github/workflows/ci.yml`: lint ➔ test ➔ duplicate-scan.  
2. Break PRs that add dup hashes or leave docs missing.

──────────────────────────────────────────
7. VICTORY LOG
──────────────────────────────────────────
On success, echo:  