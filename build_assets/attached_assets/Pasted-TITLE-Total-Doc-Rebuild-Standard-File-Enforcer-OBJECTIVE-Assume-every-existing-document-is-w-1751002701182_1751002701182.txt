TITLE: “Total Doc Rebuild + Standard-File Enforcer”

OBJECTIVE
• Assume every existing document is wrong or harmful.  
• Re-scan every `.kt`, route, and schema; rebuild documentation from live code.  
• Verify that ALL standard repo docs exist; create/patch any that are missing.  
• Keep filenames/paths intact; overwrite only contents.

TASK LIST
1. 🧹 BACKUP
   └─ Copy current `docs/` and root Markdown files to `/docs-backup-$(date)`.

2. 🔍 SOURCE-DRIVEN DOCS
   ├─ **Run detekt** full scan → `/generated-docs/detekt.html`.  📈  
   ├─ **Generate Dokka** (`dokkaHtml`, `dokkaGfm`).  📚  
   ├─ **Emit OpenAPI 3.1** from Ktor; host Swagger UI at `/docs/api`.  
   └─ **Dump Mermaid** ER (`schema.mmd`) + sequence diagrams.

3. 📄 STANDARD DOC INVENTORY
   For each of these special files, if missing, generate from best-practice templates:
   • `README.md`  → project intro, setup, badges.  
   • `CONTRIBUTING.md` → PR process, coding style.  
   • `LICENSE`    → use current license or prompt author.  
   • `CODE_OF_CONDUCT.md` → Contributor Covenant v2.1.  
   • `SECURITY.md` → responsible-disclosure guidelines.  
   • `CHANGELOG.md` → Keep-a-Changelog format.  
   • `ARCHITECTURE.md` → embed Mermaid diagrams and system overview.  
   Mark any unknown license as TODO for maintainer review.

4. 🔗 QUALITY GATES
   ├─ Run `markdown-link-check` across `/generated-docs` & root markdown.  
   └─ Fail build on dead links or Lighthouse score < 90 (perf & a11y) on `/docs`.

5. ⚙ CI PIPELINE
   └─ Add `docs.yml` GitHub Action: runs Tasks 2-4 on every push, deploys to `gh-pages`.

6. 📝 MERGE & CHANGELOG
   ├─ Replace bodies of existing files, preserve front-matter.  
   └─ Append CHANGELOG entry: “Full code-synced doc rebuild $(date)”.

ACCEPTANCE CRITERIA
✓ Dokka HTML & GFM render without 404s.  
✓ Swagger UI live at `/docs/api`.  
✓ `README`, `CONTRIBUTING`, `LICENSE`, `CODE_OF_CONDUCT`, `SECURITY`, `CHANGELOG`, `ARCHITECTURE` all present and up-to-date.  
✓ detekt report shows zero **defect** severities.  
✓ Markdown link checker passes.  
✓ Lighthouse ≥ 90 for performance + a11y.  
✓ CI green on first run.

END OF PROMPT